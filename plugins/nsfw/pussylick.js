// @nombre: pussylick
// @alias: lamercono, comer, cunnilingus
// @categoria: nsfw
// @descripcion: Lamer el coño
// @reaccion: 👅

import { reactionAction } from '../../src/lib/utils.js';

export default async function(m, ctx) {
  await reactionAction(m, ctx, 'nsfw', 'pussylick', ["le está lamiendo rico a","le está comiendo la intimidad a","se está deleitando con"]);
}
