// @nombre: lickpussy
// @categoria: nsfw
// @descripcion: Lamer coño
// @reaccion: 👅

import { reactionAction } from '../../src/lib/utils.js';

export default async function(m, ctx) {
  await reactionAction(m, ctx, 'nsfw', 'lickpussy', ["le está chupando el coño a","saborea su intimidad con","está comiendo el pastel a","le lame el clítoris a"]);
}
