// @nombre: anal
// @categoria: nsfw
// @descripcion: Anal
// @reaccion: 🔞

import { reactionAction } from '../../src/lib/utils.js';

export default async function(m, ctx) {
  await reactionAction(m, ctx, 'nsfw', 'anal', ["le está haciendo un anal a","está disfrutando un anal con","dominando por detrás a","le da duro por el asterisco a","le explora el túnel a"]);
}
