// @nombre: suckboobs
// @categoria: nsfw
// @descripcion: Chupar tetas
// @reaccion: 👅

import { reactionAction } from '../../src/lib/utils.js';

export default async function(m, ctx) {
  await reactionAction(m, ctx, 'nsfw', 'suckboobs', ["le está chupando las tetas a","succiona sus pechos a","disfruta de sus tetas a","le muerde los pezones a"]);
}
