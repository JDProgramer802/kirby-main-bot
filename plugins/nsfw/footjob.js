// @nombre: footjob
// @categoria: nsfw
// @descripcion: Footjob
// @reaccion: 🦶

import { reactionAction } from '../../src/lib/utils.js';

export default async function(m, ctx) {
  await reactionAction(m, ctx, 'nsfw', 'footjob', ["le hace un footjob a","le menea el pene con los pies a","disfruta de sus pies con","usa sus plantas para calentar a"]);
}
