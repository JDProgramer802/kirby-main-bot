// @nombre: boobjob
// @categoria: nsfw
// @descripcion: Rusa
// @reaccion: 🍒

import { reactionAction } from '../../src/lib/utils.js';

export default async function(m, ctx) {
  await reactionAction(m, ctx, 'nsfw', 'boobjob', ["le está haciendo una rusa a","le aprieta el pene con sus tetas a","disfruta entre sus pechos con","le hace un pajote con las tetas a"]);
}
